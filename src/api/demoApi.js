/**
 * 12371党员咨询服务 api
 * @example 在单页面引入api import { partyMemberConsultationApi } from '@/js/api'
 */
import { prefixUrl } from './common'
const path = '/a'

const demoApi = {
  getWebpack: `${prefixUrl}${path}/1190000018704305`, //
  getBalbel: `${prefixUrl}${path}/1190000016458913` //
}
export default demoApi
